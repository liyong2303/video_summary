---
phase: 01-project-skeleton-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - video-summary-frontend/package.json
  - video-summary-frontend/vite.config.ts
  - video-summary-frontend/tsconfig.json
  - video-summary-frontend/src/main.ts
  - video-summary-frontend/src/App.vue
  - video-summary-frontend/index.html
  - video-summary-service/pom.xml
  - video-summary-service/src/main/java/com/videosummary/VideoSummaryApplication.java
  - video-summary-service/src/main/resources/application.yml
  - video-summary-service/src/main/resources/schema.sql
  - video-summary-service/src/main/java/com/videosummary/config/MyBatisPlusConfig.java
  - video-summary-service/src/main/java/com/videosummary/controller/HealthController.java
  - video-summary-service/src/main/java/com/videosummary/entity/Task.java
  - video-summary-service/src/main/java/com/videosummary/mapper/TaskMapper.java
  - video-summary-ai/main.py
  - video-summary-ai/requirements.txt
  - video-summary-ai/app/__init__.py
  - video-summary-ai/app/main.py
autonomous: true
requirements:
  - TASK-01
  - TASK-02
  - TASK-04

must_haves:
  truths:
    - "Vue 3 frontend starts with npm run dev and shows a page"
    - "Spring Boot service starts and responds to GET /health"
    - "FastAPI service starts and responds to GET /health"
    - "Task table exists in MySQL with all required columns"
  artifacts:
    - path: "video-summary-frontend/package.json"
      provides: "Vue 3 project with Vite, TypeScript, Element Plus"
      contains: "\"vue\":"
    - path: "video-summary-service/pom.xml"
      provides: "Spring Boot 3.2 project with MyBatis-Plus, MySQL, Redis"
      contains: "mybatis-plus"
    - path: "video-summary-service/src/main/resources/schema.sql"
      provides: "Database schema for task table"
      contains: "CREATE TABLE"
    - path: "video-summary-ai/requirements.txt"
      provides: "FastAPI project with uvicorn"
      contains: "fastapi"
  key_links:
    - from: "video-summary-service/src/main/java/com/videosummary/entity/Task.java"
      to: "video-summary-service/src/main/resources/schema.sql"
      via: "field-to-column mapping"
      pattern: "TableName.*task"
---

<objective>
Create the three-service project skeleton with database schema and health check endpoints.

Purpose: Establish the foundational project structure so that subsequent plans can build domain logic (Bilibili extraction, task management) on top of working, runnable services.

Output: Three runnable service projects (Vue 3, Spring Boot, FastAPI) with health check endpoints and the task database table defined.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create three service project skeletons</name>
  <files>
    video-summary-frontend/package.json,
    video-summary-frontend/vite.config.ts,
    video-summary-frontend/tsconfig.json,
    video-summary-frontend/tsconfig.node.json,
    video-summary-frontend/index.html,
    video-summary-frontend/src/main.ts,
    video-summary-frontend/src/App.vue,
    video-summary-frontend/src/vite-env.d.ts,
    video-summary-frontend/.gitignore,
    video-summary-service/pom.xml,
    video-summary-service/src/main/java/com/videosummary/VideoSummaryApplication.java,
    video-summary-service/src/main/resources/application.yml,
    video-summary-service/.gitignore,
    video-summary-ai/main.py,
    video-summary-ai/requirements.txt,
    video-summary-ai/app/__init__.py,
    video-summary-ai/app/main.py,
    video-summary-ai/.gitignore
  </files>
  <read_first>
    (No existing files to read - greenfield project)
  </read_first>
  <action>
Create three project directories under G:\code\video_summary\:

**1. Vue 3 Frontend: video-summary-frontend/**

Create with `npm create vite@latest video-summary-frontend -- --template vue-ts` from G:\code\video_summary, then modify:

- `package.json`: Add dependencies: `element-plus`, `axios`, `vue-router`, `@element-plus/icons-vue`. Add devDependency: `unplugin-auto-import`, `unplugin-vue-components`.
- `vite.config.ts`: Configure Element Plus auto-import with `unplugin-auto-import` and `unplugin-vue-components` plugins. Set server port to 5173, proxy `/api` to `http://localhost:8080`.
- `src/main.ts`: Import ElementPlus and its CSS (`element-plus/dist/index.css`), use `app.use(ElementPlus)`.
- `src/App.vue`: Create a minimal layout with a header containing title "VideoSum" and a router-view in the main area. Use Element Plus `el-container`, `el-header`, `el-main`.
- `index.html`: Set title to "VideoSum - B站视频内容再生产平台".

**2. Spring Boot Service: video-summary-service/**

Create the Maven project structure manually (do NOT use Spring Initializr CLI - it may not be available):

- `pom.xml`: Spring Boot 3.2.5 parent, Java 17, dependencies: spring-boot-starter-web, mybatis-plus-spring-boot3-starter (version 3.5.6), mysql-connector-j, spring-boot-starter-data-redis, lombok, hutool-all (version 5.8.27), spring-boot-starter-test. Group ID: com.videosummary, artifact: video-summary-service.
- `VideoSummaryApplication.java`: Standard `@SpringBootApplication` main class with `SpringApplication.run`.
- `application.yml`: Configure:
  - server.port=8080
  - spring.datasource.url=jdbc:mysql://localhost:3306/video_summary?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
  - spring.datasource.username=root
  - spring.datasource.password=root
  - spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
  - mybatis-plus.configuration.map-underscore-to-camel-case=true
  - mybatis-plus.global-config.db-config.logic-delete-field=deleted
  - spring.data.redis.host=localhost
  - spring.data.redis.port=6379
  - app.internal-secret (placeholder: change-me-in-production)
  - app.bilibili.cookie (placeholder: your-bilibili-cookie-here)
  - app.video.duration-limit-minutes=30
- `.gitignore`: Standard Java/Maven gitignore (target/, *.class, .idea/, *.iml, etc.)

**3. FastAPI AI Service: video-summary-ai/**

- `requirements.txt`: fastapi==0.111.0, uvicorn[standard]==0.30.1, python-dotenv==1.0.1, httpx==0.27.0
- `app/__init__.py`: Empty file
- `app/main.py`: FastAPI app with `GET /health` endpoint returning `{"status": "ok", "service": "video-summary-ai"}`. Include CORS middleware allowing all origins (for development).
- `main.py`: Entry point that runs `uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload`.
- `.gitignore`: Standard Python gitignore (__pycache__/, *.pyc, .env, venv/, etc.)

After creating all files, run `cd video-summary-frontend && npm install` to install frontend dependencies.
  </action>
  <verify>
    <automated>cd G:/code/video_summary/video-summary-frontend && test -f package.json && grep -q '"vue"' package.json && echo "FRONTEND_OK" || echo "FRONTEND_FAIL"</automated>
    <automated>test -f G:/code/video_summary/video-summary-service/pom.xml && grep -q 'mybatis-plus' G:/code/video_summary/video-summary-service/pom.xml && echo "SERVICE_OK" || echo "SERVICE_FAIL"</automated>
    <automated>test -f G:/code/video_summary/video-summary-ai/requirements.txt && grep -q 'fastapi' G:/code/video_summary/video-summary-ai/requirements.txt && echo "AI_OK" || echo "AI_FAIL"</automated>
  </verify>
  <done>
    - video-summary-frontend/ has package.json with vue, element-plus, vue-router dependencies and node_modules installed
    - video-summary-service/ has pom.xml with Spring Boot 3.2.5, mybatis-plus, mysql, redis dependencies
    - video-summary-ai/ has requirements.txt with fastapi, uvicorn, httpx
    - Each project has a valid entry point (main.ts, VideoSummaryApplication.java, app/main.py)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create database schema, MyBatis-Plus config, Task entity, and health endpoint</name>
  <files>
    video-summary-service/src/main/resources/schema.sql,
    video-summary-service/src/main/java/com/videosummary/config/MyBatisPlusConfig.java,
    video-summary-service/src/main/java/com/videosummary/controller/HealthController.java,
    video-summary-service/src/main/java/com/videosummary/entity/Task.java,
    video-summary-service/src/main/java/com/videosummary/mapper/TaskMapper.java
  </files>
  <read_first>
    video-summary-service/src/main/resources/application.yml
    video-summary-service/src/main/java/com/videosummary/VideoSummaryApplication.java
  </read_first>
  <action>
Create the following files in video-summary-service:

**1. schema.sql** at `src/main/resources/schema.sql`:
```sql
CREATE DATABASE IF NOT EXISTS video_summary DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE video_summary;

CREATE TABLE IF NOT EXISTS task (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    user_id BIGINT NOT NULL DEFAULT 0 COMMENT '用户ID（Phase 1暂用0表示匿名）',
    bvid VARCHAR(20) NOT NULL COMMENT 'BV号',
    video_title VARCHAR(200) DEFAULT NULL COMMENT '视频标题',
    video_duration INT DEFAULT NULL COMMENT '视频时长（秒）',
    cover_url VARCHAR(500) DEFAULT NULL COMMENT '封面URL',
    subtitle_storage_path VARCHAR(500) DEFAULT NULL COMMENT '字幕文件存储路径',
    status VARCHAR(20) NOT NULL DEFAULT 'pending' COMMENT '任务状态: pending/processing/completed/partially_completed/failed/cancelled',
    error_message VARCHAR(500) DEFAULT NULL COMMENT '错误信息',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    completed_at DATETIME DEFAULT NULL COMMENT '完成时间',
    UNIQUE KEY uk_user_bvid (user_id, bvid) COMMENT '同用户同BV号去重',
    INDEX idx_status (status) COMMENT '状态索引',
    INDEX idx_created_at (created_at) COMMENT '创建时间索引'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='任务表';
```

**2. MyBatisPlusConfig.java** at `src/main/java/com/videosummary/config/MyBatisPlusConfig.java`:
```java
@Configuration
@MapperScan("com.videosummary.mapper")
public class MyBatisPlusConfig {
    @Bean
    public MybatisPlusInterceptor mybatisPlusInterceptor() {
        MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();
        interceptor.addInnerInterceptor(new PaginationInnerInterceptor(DbType.MYSQL));
        return interceptor;
    }
}
```

**3. Task.java** at `src/main/java/com/videosummary/entity/Task.java`:
- Annotate with `@Data`, `@TableName("task")`, `@Builder`, `@NoArgsConstructor`, `@AllArgsConstructor`
- Fields: `id` (Long, `@TableId(type=IdType.AUTO)`), `userId` (Long), `bvid` (String), `videoTitle` (String), `videoDuration` (Integer), `coverUrl` (String), `subtitleStoragePath` (String), `status` (String, default "pending"), `errorMessage` (String), `createdAt` (LocalDateTime, `@TableField(fill=FieldFill.INSERT)`), `completedAt` (LocalDateTime)
- Add a static inner class `Status` with constants: `PENDING = "pending"`, `PROCESSING = "processing"`, `COMPLETED = "completed"`, `PARTIALLY_COMPLETED = "partially_completed"`, `FAILED = "failed"`, `CANCELLED = "cancelled"`

**4. TaskMapper.java** at `src/main/java/com/videosummary/mapper/TaskMapper.java`:
- Interface extending `BaseMapper<Task>`
- Annotate with `@Mapper`

**5. HealthController.java** at `src/main/java/com/videosummary/controller/HealthController.java`:
- `@RestController`, `@RequestMapping`
- `GET /health` returns `{"status": "ok", "service": "video-summary-service"}`

Also add `spring.sql.init.mode=always` and `spring.sql.init.schema-locations=classpath:schema.sql` to application.yml so the schema runs on startup. Set `spring.sql.init.mode=never` as default (schema should be applied manually or via migration tool; for dev, set to `always` but document that it should be changed for production). Actually, set it to `never` by default and add a comment in application.yml that developers should run schema.sql manually for first-time setup. This avoids accidentally re-creating tables on restart.
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE" G:/code/video_summary/video-summary-service/src/main/resources/schema.sql</automated>
    <automated>grep -q "uk_user_bvid" G:/code/video_summary/video-summary-service/src/main/resources/schema.sql && echo "DEDUP_OK" || echo "DEDUP_FAIL"</automated>
    <automated>grep -q "@TableName" G:/code/video_summary/video-summary-service/src/main/java/com/videosummary/entity/Task.java && echo "ENTITY_OK" || echo "ENTITY_FAIL"</automated>
    <automated>grep -q "BaseMapper" G:/code/video_summary/video-summary-service/src/main/java/com/videosummary/mapper/TaskMapper.java && echo "MAPPER_OK" || echo "MAPPER_FAIL"</automated>
    <automated>grep -q "/health" G:/code/video_summary/video-summary-service/src/main/java/com/videosummary/controller/HealthController.java && echo "HEALTH_OK" || echo "HEALTH_FAIL"</automated>
  </verify>
  <done>
    - schema.sql defines task table with all columns including id, user_id, bvid, video_title, video_duration, cover_url, subtitle_storage_path, status, error_message, created_at, completed_at
    - Unique constraint (uk_user_bvid) on (user_id, bvid) supports TASK-02 deduplication
    - Task.java entity has @TableName("task"), @Data, all fields with correct types, and Status constants for all 6 states
    - TaskMapper extends BaseMapper<Task>
    - MyBatisPlusConfig configures pagination interceptor and mapper scanning
    - HealthController exposes GET /health returning status JSON
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Spring Boot | Untrusted input from public API calls (BV号 submission) |
| Spring Boot → MySQL | Internal service to database, credentials in config |
| Spring Boot → Redis | Internal service to cache, credentials in config |
| Spring Boot → FastAPI | Internal service-to-service via X-Internal-Secret header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | S | POST /api/video/submit | mitigate | Input validation on BV号 format (regex: BV[A-Za-z0-9]+) before processing (implemented in Plan 03) |
| T-01-02 | I | Spring Boot → MySQL | accept | Local development; production will use encrypted connections and credential management |
| T-01-03 | D | POST /api/video/submit | mitigate | Rate limiting via Redis in future phase; unique constraint prevents duplicate task spam |
| T-01-04 | S | Spring Boot → FastAPI | mitigate | X-Internal-Secret header validated on both sides; secret stored in application.yml not hardcoded |
</threat_model>

<verification>
1. `cd video-summary-frontend && npm run build` exits 0 (compiles without errors)
2. Spring Boot project compiles: `cd video-summary-service && mvn compile -q` exits 0
3. FastAPI app loads: `cd video-summary-ai && python -c "from app.main import app; print('OK')"` exits 0
4. schema.sql is valid SQL (check with `mysql --help` or manual review)
5. Task entity fields match schema.sql columns
</verification>

<success_criteria>
- Vue 3 project builds successfully with npm run build
- Spring Boot project compiles with mvn compile
- FastAPI app can be imported without errors
- schema.sql defines task table with unique constraint on (user_id, bvid)
- Task.java entity maps to task table with all 6 status constants
- GET /health endpoint defined in both Spring Boot and FastAPI
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-skeleton-b/01-01-SUMMARY.md`
</output>
