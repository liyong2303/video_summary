# v1.1 实现计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans

## 版本目标

生成内容不再只读，用户能修改保存；基础体验更顺滑。

---

## 待办清单（Roadmap 中定义）

| 编号 | 功能 | 说明 |
|------|------|------|
| EDIT-01 | 内容在线编辑 | 结果页 Tab 变为可编辑文本框，保存回 DB |
| EDIT-02 | 编辑历史记录 | 每次保存前记录旧版本，保留最近3版，可回滚 |
| UX-01 | 移动端适配 | 基础响应式布局，手机能查看历史和复制内容 |
| UX-02 | 错误提示优化 | API 超时、字幕缺失、额度超限均有明确提示 |
| UX-03 | 加载状态细化 | 各步骤显示进度（"正在生成总结…" → "正在并行生成文章/卡片…"） |

---

## 架构分析

### 现状
- **数据库**: `task_result` 表存储生成内容，无历史记录
- **后端**: `VideoController` 提供查询、重新生成、导出接口，无编辑接口
- **前端**: `TaskResultView.vue` 和 `SubmitView.vue` 显示只读内容（`div.result-text`）
- **加载状态**: `StreamService` SSE 事件已有 `step_start` / `step_complete`，但前端显示模糊（"正在生成..."）

### 需要的变更
1. **数据库**: 新增 `task_result_history` 表记录编辑历史
2. **后端**: 新增编辑、历史查询、回滚接口
3. **前端**: 将只读文本框改为可编辑，添加版本历史 UI，优化加载提示，添加响应式样式
4. **错误处理**: 补充 GlobalExceptionHandler 缺失场景

---

## 详细实现计划

### Task 1: 数据库变更 — 新增历史记录表

#### 1.1 创建 `task_result_history` 表

```sql
CREATE TABLE IF NOT EXISTS task_result_history (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    task_result_id BIGINT NOT NULL COMMENT '关联的 task_result.id',
    task_id BIGINT NOT NULL COMMENT '任务ID（冗余字段，方便查询）',
    output_type VARCHAR(20) NOT NULL COMMENT '输出类型（冗余字段）',
    content LONGTEXT COMMENT '内容快照',
    version INT NOT NULL COMMENT '版本号（从1开始递增）',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    INDEX idx_task_result_id (task_result_id),
    INDEX idx_task_id_output_type (task_id, output_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='任务结果编辑历史表';
```

#### 1.2 更新 `schema.sql`
- 将上述 DDL 添加到 `schema.sql` 末尾

**验证**: 执行 SQL，表创建成功

---

### Task 2: 后端 — TaskResultHistory 实体和 Mapper

#### 2.1 创建 `TaskResultHistory.java` 实体

**路径**: `video-summary-service/src/main/java/com/videosummary/entity/TaskResultHistory.java`

```java
package com.videosummary.entity;

import com.baomidou.mybatisplus.annotation.*;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

@Data
@TableName("task_result_history")
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class TaskResultHistory {

    @TableId(type = IdType.AUTO)
    private Long id;

    private Long taskResultId;

    private Long taskId;

    private String outputType;

    private String content;

    private Integer version;

    @TableField(fill = FieldFill.INSERT)
    private java.time.LocalDateTime createdAt;
}
```

#### 2.2 创建 `TaskResultHistoryMapper.java`

**路径**: `video-summary-service/src/main/java/com/videosummary/mapper/TaskResultHistoryMapper.java`

```java
package com.videosummary.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.videosummary.entity.TaskResultHistory;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface TaskResultHistoryMapper extends BaseMapper<TaskResultHistory> {
}
```

#### 2.3 创建 `TaskResultHistoryService.java`

**路径**: `video-summary-service/src/main/java/com/videosummary/service/TaskResultHistoryService.java`

```java
package com.videosummary.service;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.videosummary.entity.TaskResult;
import com.videosummary.entity.TaskResultHistory;
import com.videosummary.mapper.TaskResultHistoryMapper;
import com.videosummary.mapper.TaskResultMapper;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;

@Service
@RequiredArgsConstructor
public class TaskResultHistoryService {

    private final TaskResultHistoryMapper historyMapper;
    private final TaskResultMapper taskResultMapper;

    /**
     * 保存编辑历史，保留最近3版
     */
    @Transactional
    public void saveHistory(TaskResult taskResult) {
        // 查询当前版本号
        Integer currentVersion = historyMapper.selectCount(
                new LambdaQueryWrapper<TaskResultHistory>()
                        .eq(TaskResultHistory::getTaskResultId, taskResult.getId())
        );

        // 插入新历史记录
        TaskResultHistory history = TaskResultHistory.builder()
                .taskResultId(taskResult.getId())
                .taskId(taskResult.getTaskId())
                .outputType(taskResult.getOutputType())
                .content(taskResult.getContent())
                .version(currentVersion + 1)
                .build();
        historyMapper.insert(history);

        // 删除超过3版的旧记录
        if (currentVersion + 1 > 3) {
            Integer keepVersion = currentVersion + 1 - 3;
            historyMapper.delete(
                    new LambdaQueryWrapper<TaskResultHistory>()
                            .eq(TaskResultHistory::getTaskResultId, taskResult.getId())
                            .lt(TaskResultHistory::getVersion, keepVersion)
            );
        }
    }

    /**
     * 获取历史版本列表
     */
    public List<TaskResultHistory> getHistory(Long taskResultId) {
        return historyMapper.selectList(
                new LambdaQueryWrapper<TaskResultHistory>()
                        .eq(TaskResultHistory::getTaskResultId, taskResultId)
                        .orderByDesc(TaskResultHistory::getVersion)
        );
    }

    /**
     * 根据 taskId 和 outputType 获取历史版本
     */
    public List<TaskResultHistory> getHistoryByTaskAndType(Long taskId, String outputType) {
        return historyMapper.selectList(
                new LambdaQueryWrapper<TaskResultHistory>()
                        .eq(TaskResultHistory::getTaskId, taskId)
                        .eq(TaskResultHistory::getOutputType, outputType)
                        .orderByDesc(TaskResultHistory::getVersion)
        );
    }
}
```

**验证**: 编译通过，无语法错误

---

### Task 3: 后端 — 编辑、历史查询、回滚接口

#### 3.1 更新 `VideoController.java`

**新增接口**:

```java
@PutMapping("/{taskId}/result/{outputType}")
public ApiResult<Void> updateResult(
        @PathVariable Long taskId,
        @PathVariable String outputType,
        @RequestBody Map<String, String> body) {
    taskService.updateResult(taskId, outputType, body.get("content"));
    return ApiResult.success(null);
}

@GetMapping("/{taskId}/result/{outputType}/history")
public ApiResult<List<TaskResultHistory>> getResultHistory(
        @PathVariable Long taskId,
        @PathVariable String outputType) {
    return ApiResult.success(taskResultHistoryService.getHistoryByTaskAndType(taskId, outputType));
}

@PostMapping("/{taskId}/result/{outputType}/rollback/{version}")
public ApiResult<Void> rollbackResult(
        @PathVariable Long taskId,
        @PathVariable String outputType,
        @PathVariable Integer version) {
    taskService.rollbackResult(taskId, outputType, version);
    return ApiResult.success(null);
}
```

#### 3.2 更新 `TaskService.java`

**新增方法**:

```java
private final TaskResultHistoryService taskResultHistoryService;

@Transactional
public void updateResult(Long taskId, String outputType, String content) {
    // 查找现有结果
    TaskResult result = taskResultMapper.selectOne(
            new LambdaQueryWrapper<TaskResult>()
                    .eq(TaskResult::getTaskId, taskId)
                    .eq(TaskResult::getOutputType, outputType)
    );
    if (result == null) {
        throw new IllegalArgumentException("结果不存在");
    }

    // 保存历史
    taskResultHistoryService.saveHistory(result);

    // 更新内容
    result.setContent(content);
    taskResultMapper.updateById(result);
}

@Transactional
public void rollbackResult(Long taskId, String outputType, Integer version) {
    // 查找历史版本
    TaskResultHistory history = taskResultHistoryMapper.selectOne(
            new LambdaQueryWrapper<TaskResultHistory>()
                    .eq(TaskResultHistory::getTaskId, taskId)
                    .eq(TaskResultHistory::getOutputType, outputType)
                    .eq(TaskResultHistory::getVersion, version)
    );
    if (history == null) {
        throw new IllegalArgumentException("历史版本不存在");
    }

    // 查找当前结果
    TaskResult result = taskResultMapper.selectOne(
            new LambdaQueryWrapper<TaskResult>()
                    .eq(TaskResult::getTaskId, taskId)
                    .eq(TaskResult::getOutputType, outputType)
    );
    if (result == null) {
        throw new IllegalArgumentException("结果不存在");
    }

    // 保存当前版本到历史
    taskResultHistoryService.saveHistory(result);

    // 回滚内容
    result.setContent(history.getContent());
    taskResultMapper.updateById(result);
}
```

**验证**: 编译通过，接口可正常调用

---

### Task 4: 前端 — 内容在线编辑

#### 4.1 更新 `TaskResultView.vue`

**变更**:
1. `results` 改为响应式 ref，初始值从 API 获取
2. `result-text` div 改为 `el-input type="textarea"`（`v-model` 绑定）
3. 添加 `editing` 状态（是否正在编辑）
4. 添加 `unsavedChanges` 状态（是否有未保存的更改）
5. 添加"保存"按钮（调用 PUT 接口）
6. 添加切换 Tab 时的提示（如果有未保存更改）

**核心代码**:

```vue
<script setup lang="ts">
// 新增状态
const editing = ref<Record<string, boolean>>({})
const unsavedChanges = ref<Record<string, boolean>>({})

// 保存编辑
async function saveEdit(outputType: string) {
  try {
    const id = route.params.id
    await axios.put(`/api/video/${id}/result/${outputType}`, {
      content: results.value[outputType]
    })
    unsavedChanges.value[outputType] = false
    editing.value[outputType] = false
    ElMessage.success('保存成功')
  } catch (e: any) {
    ElMessage.error(e.response?.data?.message || '保存失败')
  }
}

// 检测内容变化
watch(() => results.value, (newVal, oldVal) => {
  for (const key in newVal) {
    if (oldVal[key] !== newVal[key]) {
      unsavedChanges.value[key] = true
    }
  }
}, { deep: true })
</script>

<template>
  <div class="tab-content">
    <el-input
      v-model="results[key]"
      type="textarea"
      :rows="15"
      placeholder="点击编辑按钮修改内容..."
      :readonly="!editing[key]"
      @input="unsavedChanges[key] = true"
    />
    <div class="result-actions">
      <el-button
        v-if="!editing[key]"
        type="primary"
        size="small"
        @click="editing[key] = true"
      >
        编辑
      </el-button>
      <el-button
        v-if="editing[key] && unsavedChanges[key]"
        type="success"
        size="small"
        @click="saveEdit(key)"
      >
        保存
      </el-button>
      <el-button
        v-if="editing[key]"
        size="small"
        @click="editing[key] = false; fetchResults()"
      >
        取消
      </el-button>
      <el-button
        v-if="!editing[key]"
        type="default"
        size="small"
        @click="copyText(results[key])"
      >
        复制
      </el-button>
      <el-button
        size="small"
        :loading="regenerating === key"
        @click="regenerateStep(key)"
      >
        重新生成
      </el-button>
    </div>
  </div>
</template>
```

#### 4.2 更新 `SubmitView.vue`

- 同样的变更逻辑

**验证**: 前端能正常编辑、保存内容

---

### Task 5: 前端 — 编辑历史记录 UI

#### 5.1 添加 `HistoryDialog.vue` 组件

**路径**: `video-summary-frontend/src/components/HistoryDialog.vue`

```vue
<script setup lang="ts">
import { ref, watch } from 'vue'
import { ElMessage } from 'element-plus'
import axios from 'axios'

const props = defineProps<{
  visible: boolean
  taskId: number
  outputType: string
}>()

const emit = defineEmits<{
  'update:visible': [value: boolean]
  'rollback': []
}>()

const histories = ref<any[]>([])
const loading = ref(false)

async function fetchHistories() {
  loading.value = true
  try {
    const res = await axios.get(`/api/video/${props.taskId}/result/${props.outputType}/history`)
    if (res.data.code === 0) {
      histories.value = res.data.data
    }
  } catch (e) {
    console.error('Failed to fetch histories', e)
  } finally {
    loading.value = false
  }
}

async function rollback(version: number) {
  try {
    await axios.post(`/api/video/${props.taskId}/result/${props.outputType}/rollback/${version}`)
    ElMessage.success('回滚成功')
    emit('rollback')
    emit('update:visible', false)
  } catch (e: any) {
    ElMessage.error(e.response?.data?.message || '回滚失败')
  }
}

watch(() => props.visible, (val) => {
  if (val) fetchHistories()
})
</script>

<template>
  <el-dialog
    :model-value="visible"
    @update:model-value="$emit('update:visible', $event)"
    title="编辑历史"
    width="600px"
  >
    <el-timeline v-loading="loading">
      <el-timeline-item
        v-for="h in histories"
        :key="h.id"
        :timestamp="new Date(h.createdAt).toLocaleString()"
      >
        <div class="history-item">
          <div class="history-header">
            <span class="version-badge">v{{ h.version }}</span>
            <el-button
              v-if="h.version !== histories[0]?.version"
              type="primary"
              size="small"
              link
              @click="rollback(h.version)"
            >
              回滚到此版本
            </el-button>
          </div>
          <div class="history-content">{{ h.content }}</div>
        </div>
      </el-timeline-item>
    </el-timeline>
    <el-empty v-if="!loading && histories.length === 0" description="暂无历史记录" />
  </el-dialog>
</template>

<style scoped>
.version-badge {
  background: #409eff;
  color: white;
  padding: 2px 8px;
  border-radius: 4px;
  font-size: 12px;
  margin-right: 8px;
}
.history-content {
  margin-top: 8px;
  padding: 8px;
  background: #f5f7fa;
  border-radius: 4px;
  font-size: 13px;
  max-height: 100px;
  overflow: hidden;
  text-overflow: ellipsis;
}
</style>
```

#### 5.2 在 `TaskResultView.vue` 和 `SubmitView.vue` 中集成

```vue
<script setup lang="ts">
import HistoryDialog from '@/components/HistoryDialog.vue'

const historyDialogVisible = ref(false)
const currentOutputType = ref('')

function showHistory(outputType: string) {
  currentOutputType.value = outputType
  historyDialogVisible.value = true
}

function handleRollback() {
  fetchResults()
}
</script>

<template>
  <div class="result-actions">
    <!-- 现有按钮... -->
    <el-button
      size="small"
      @click="showHistory(key)"
    >
      历史版本
    </el-button>
  </div>

  <HistoryDialog
    v-model:visible="historyDialogVisible"
    :task-id="Number(task?.id)"
    :output-type="currentOutputType"
    @rollback="handleRollback"
  />
</template>
```

**验证**: 能查看历史版本列表，回滚功能正常

---

### Task 6: 前端 — 移动端适配（响应式布局）

#### 6.1 更新 `TaskResultView.vue` 样式

```css
.task-result-view {
  max-width: 800px;
  margin: 0 auto;
  padding: 40px 20px;
}

/* 移动端适配 */
@media (max-width: 768px) {
  .task-result-view {
    padding: 20px 12px;
  }

  .video-info {
    flex-direction: column;
  }

  .cover {
    width: 100%;
    height: auto;
    aspect-ratio: 16/9;
  }

  .result-text {
    max-height: 300px;
    font-size: 13px;
  }

  .result-actions {
    flex-direction: column;
  }

  .result-actions .el-button {
    width: 100%;
  }
}
```

#### 6.2 更新 `SubmitView.vue` 样式

```css
.submit-view {
  max-width: 800px;
  margin: 0 auto;
  padding: 40px 20px;
}

@media (max-width: 768px) {
  .submit-view {
    padding: 20px 12px;
  }

  .input-row {
    flex-direction: column;
  }

  .video-info {
    flex-direction: column;
  }

  .cover {
    width: 100%;
    height: auto;
    aspect-ratio: 16/9;
  }
}
```

#### 6.3 更新 `HistoryView.vue` 样式

```css
/* 原有样式... */

@media (max-width: 768px) {
  .history-view {
    padding: 20px 12px;
  }

  .video-info {
    flex-direction: column;
  }

  .cover {
    width: 100%;
    height: auto;
    aspect-ratio: 16/9;
  }
}
```

**验证**: 在手机视口（375px）下布局正常，按钮可点击

---

### Task 7: 后端 — 错误提示优化

#### 7.1 更新 `GlobalExceptionHandler.java`

**新增异常处理器**:

```java
@ResponseStatus(HttpStatus.REQUEST_TIMEOUT)
@ExceptionHandler(org.springframework.web.client.ResourceAccessException.class)
public ApiResult<Void> handleRequestTimeout(ResourceAccessException e) {
    return ApiResult.error(408, "AI 服务请求超时，请稍后重试");
}

@ResponseStatus(HttpStatus.REQUEST_TIMEOUT)
@ExceptionHandler(java.util.concurrent.TimeoutException.class)
public ApiResult<Void> handleTimeout(TimeoutException e) {
    return ApiResult.error(408, "处理超时，请稍后重试");
}

@ResponseStatus(HttpStatus.SERVICE_UNAVAILABLE)
@ExceptionHandler(org.springframework.dao.DataAccessException.class)
public ApiResult<Void> handleDataAccess(DataAccessException e) {
    log.error("Database error", e);
    return ApiResult.error(503, "数据库服务暂时不可用");
}

@ResponseStatus(HttpStatus.BAD_REQUEST)
@ExceptionHandler(cn.dev33.satoken.exception.NotLoginException.class)
public ApiResult<Void> handleNotLogin(NotLoginException e) {
    return ApiResult.error(401, "请先登录");
}
```

**验证**: 各异常场景返回正确的 HTTP 状态码和错误消息

---

### Task 8: 前端 — 加载状态细化

#### 8.1 更新 `StreamService.java`（后端 SSE 事件优化）

**在 `processAndStream` 方法中添加更细粒度的进度**:

```java
// Step 1: Extract subtitles
sendEvent(emitter, "step_start", Map.of(
    "step", "extract",
    "message", "正在提取B站字幕..."
));

// ... 提取字幕逻辑 ...

sendEvent(emitter, "step_complete", Map.of(
    "step", "extract",
    "status", "completed",
    "message", "字幕提取完成"
));

// Step 2: AI pipeline - summary
sendEvent(emitter, "step_start", Map.of(
    "step", "summary",
    "message", "正在生成总结..."
));

// ... 流式处理总结 ...

sendEvent(emitter, "step_complete", Map.of(
    "step", "summary",
    "status", "completed",
    "message", "总结生成完成"
));

// Step 3: AI pipeline - parallel steps
sendEvent(emitter, "step_start", Map.of(
    "step", "parallel",
    "message", "正在并行生成文章、学习卡片、小红书文案..."
));

// ... 流式处理并行步骤 ...
```

#### 8.2 更新 `SubmitView.vue`（前端加载提示优化）

```typescript
const stepMessages: Record<string, string> = {
  extract: '正在提取B站字幕...',
  summary: '正在生成总结...',
  article: '正在生成文章...',
  card: '正在生成学习卡片...',
  xiaohongshu: '正在生成小红书文案...',
  parallel: '正在并行生成文章、学习卡片、小红书文案...'
}

// 在 connectSSE 的 onmessage 中处理 message 字段
case 'step_start':
  streamingStep.value = data.step
  if (data.message) {
    loadingMessage.value = data.message
  } else {
    loadingMessage.value = stepMessages[data.step] || '处理中...'
  }
  break

case 'step_complete':
  if (data.message) {
    loadingMessage.value = data.message
  }
  // 等待1秒后清空提示
  setTimeout(() => {
    streamingStep.value = ''
    loadingMessage.value = ''
  }, 1000)
  break
```

```vue
<!-- 更新 streaming-indicator -->
<div v-if="isStreaming" class="streaming-indicator">
  <el-icon class="is-loading"><Loading /></el-icon>
  <span>{{ loadingMessage || '处理中...' }}</span>
</div>
```

**验证**: 加载过程中显示具体的步骤提示（"正在生成总结..." 等）

---

## 执行顺序

1. Task 1: 数据库变更（创建历史表）
2. Task 2: 后端实体和 Mapper
3. Task 3: 后端接口（编辑、历史、回滚）
4. Task 4: 前端在线编辑
5. Task 5: 前端历史记录 UI
6. Task 6: 前端响应式布局
7. Task 7: 后端错误提示优化
8. Task 8: 前端加载状态细化

---

## 交付标准

- [ ] 用户能在结果页直接修改生成内容并保存
- [ ] 每次编辑自动保存历史，保留最近3版
- [ ] 能查看历史版本列表并回滚
- [ ] 手机端能正常查看、复制、编辑内容
- [ ] 所有错误场景（超时、字幕缺失、额度超限）有明确提示
- [ ] 加载过程显示具体步骤（"正在提取B站字幕..." → "正在生成总结..." 等）

---

## 验收方式

1. **编辑功能测试**:
   - 打开任务结果页，点击"编辑"，修改内容，点击"保存"
   - 刷新页面，确认修改已持久化

2. **历史记录测试**:
   - 编辑内容3次，点击"历史版本"，确认显示3个版本
   - 点击"回滚到此版本"，确认内容恢复

3. **移动端测试**:
   - 使用浏览器开发者工具切换到手机视图（375px）
   - 确认布局正常，按钮可点击

4. **错误提示测试**:
   - 故意关闭 AI 服务，提交任务，确认显示"AI服务请求超时"
   - 提交不存在的 BV 号，确认显示"B站API调用失败"

5. **加载状态测试**:
   - 提交任务，观察加载提示（"正在提取B站字幕..." → "正在生成总结..." 等）
